package org.mg.mindgraph.scheduler;

/*
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.mg.mindgraph.service.NotionSyncService;
import org.springframework.scheduling.annotation.EnableScheduling;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

@Slf4j
@Component
@EnableScheduling
@RequiredArgsConstructor
public class NotionSyncScheduler {

    private final NotionSyncService notionSyncService;

    
     * application.properties에 정의된 주기에 따라 Notion 데이터베이스 동기화를 실행합니다.
     * 기본값은 1시간입니다.
     
    @Scheduled(fixedRateString = "${mindgraph.notion.sync-interval-ms:3600000}")
    public void scheduleNotionSync() {
        log.info("Executing scheduled Notion database sync.");
        notionSyncService.syncDatabase();
    }
}
*/
