spring.application.name=MindGraph
# 서버 포트 (기본 8080)
server.port=8080

# Database 설정 (Docker의 PostgreSQL 사용)
spring.datasource.url=jdbc:postgresql://localhost:5432/mindgraph_db
spring.datasource.username=mindgraph_user
spring.datasource.password=mindgraph_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# LangChain4j Ollama 설정 (Qwen 2.5 14B)
langchain4j.ollama.chat-model.base-url=http://localhost:11434
langchain4j.ollama.chat-model.model-name=qwen2.5:14b
langchain4j.ollama.chat-model.temperature=0.2

# 임베딩 모델 설정 (mxbai-embed-large)
langchain4j.ollama.embedding-model.base-url=http://localhost:11434
langchain4j.ollama.embedding-model.model-name=mxbai-embed-large

# RabbitMQ Connection Properties
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# RabbitMQ Listener Retry and DLQ Configuration
spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.retry.initial-interval=2000ms
spring.rabbitmq.listener.simple.retry.multiplier=2.0
# 재시도 모두 실패 시 DLQ로 보내기
spring.rabbitmq.listener.simple.default-requeue-rejected=false

# File Watcher Service Configuration
mindgraph.file-watcher.directory=./watched-files

# Notion API Integration Configuration (Temporarily Disabled)
# 🚨 Replace with your actual Notion Integration Token (API Key)
# mindgraph.notion.api-key=YOUR_NOTION_API_KEY_HERE
# 🚨 Replace with the ID of the Notion database you want to sync
# mindgraph.notion.database-id=YOUR_NOTION_DATABASE_ID_HERE
# Sync interval in milliseconds (e.g., 3600000 = 1 hour)
# mindgraph.notion.sync-interval-ms=3600000

# Neo4j Connection Properties
spring.neo4j.uri=bolt://localhost:7687
spring.neo4j.authentication.username=neo4j
spring.neo4j.authentication.password=password
